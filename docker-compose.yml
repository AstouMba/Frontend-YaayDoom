services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8000/api
    container_name: yaaydoom-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
